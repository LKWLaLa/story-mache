 <br><br>
 <div class="column_10 bck dark padding padding-bottom">

    <form name="form" class="column_9">
                
        Add to story here!
        <textarea
            type="text"
            name="body"
            required="required"
            ng-model="contribution.body"
            placeholder="Your addition..."></textarea>

        <div ng-if="form.body.$touched">
            <div ng-if="form.body.$error.required">
                Content is required.
            </div>
        </div><br>
    
    
        Author name:
        <input
            type="text"
            name="author"
            maxlength="15"
            required="required"
            ng-model="contribution.author" />

        <div ng-if="form.author.$touched">
            <div ng-if="form.author.$error.required">
                Name is required.
            </div>                        
        </div><br>
    
    
        <input
            type="button"
            name="submit"
            value="submit"
            ng-disabled="form.$invalid"
            ng-click="contribution.submit()" />
           

    </form><br>
 </div>